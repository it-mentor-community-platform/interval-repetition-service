spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/it_mentor_community_platform?currentSchema=interval_repetition_service
    username: ${POSTGRES_USERNAME:root}
    password: ${POSTGRES_PASSWORD:password}


server:
  port: 8088